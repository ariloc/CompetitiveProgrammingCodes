#include <bits/stdc++.h>

#define forn(i,n) for(int i = 0; i < int(n); i++)

using namespace std;

int main() {
    freopen("empaque.in","r",stdin);

    cin >> n;
    forn (i,n) {

    }
    return 0;
}
